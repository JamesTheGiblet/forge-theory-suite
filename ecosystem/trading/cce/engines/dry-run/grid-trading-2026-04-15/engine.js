const initSqlJs = require("sql.js"); const fs = require("fs"); const path = require("path"); const DB_PATH = path.join(process.env.HOME, "kraken-intelligence/data/intelligence.db"); class Engine { constructor() { this.id = null; this.mode = null; this.capital = 0; this.position = null; this.params = { targetPct: 0.5, stopPct: 0.3, maxHoldDays: 3 }; } async start(config) { this.id = config.id; this.mode = config.mode; this.capital = config.capital; console.log("Started " + this.id + " in " + this.mode + " mode with $" + this.capital); this.monitor(); } async monitor() { setInterval(() => {}, 60000); } stop() { console.log("Stopped " + this.id); } } module.exports = Engine;